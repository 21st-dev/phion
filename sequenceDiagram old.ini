sequenceDiagram
    participant U as 👤 Пользователь
    participant B as 🌐 Браузер Next.js порт 3004
    participant V as 💻 VSCode или Cursor редактор
    participant W as 📡 shipvibes-dev.js агент слежения за файлами
    participant WS as 🔄 WebSocket сервер порт 8080
    participant DB as 🗃️ База данных Supabase PostgreSQL
    participant R2 as 📦 Файловое хранилище Cloudflare R2
    participant N as 🔥 Сервис деплоя Netlify

    Note over U,N: 🚀 ПОДРОБНЫЙ WORKFLOW SHIPVIBES - КАК ВСЕ РАБОТАЕТ ОТ НАЧАЛА ДО КОНЦА

    %% === 1. СОЗДАНИЕ ПРОЕКТА ===
    rect rgb(240, 248, 255)
        Note over U,B: 1️⃣ СОЗДАНИЕ ПРОЕКТА - пользователь создает новый проект на платформе
        U->>B: Заходит на страницу /dashboard в браузере
        Note right of U: Пользователь видит свой личный кабинет с проектами
        B->>B: Загружается dashboard.tsx компонент интерфейса
        
        U->>B: Нажимает кнопку "Create Project" и вводит название
        Note right of U: Выбирает шаблон проекта например React + Vite
        B->>B: Вызывается API POST /api/projects из файла route.ts
        Note right of B: Сервер начинает создавать проект в базе данных
        
        B->>DB: Функция createProject создает новую запись в таблице projects
        Note right of DB: Сохраняется название проекта и привязка к пользователю
        DB->>DB: INSERT INTO projects с полями id name user_id template_type
        DB-->>B: Возвращает созданный объект проекта с уникальным ID
        
        B->>B: Функция generateAndUploadTemplate создает ZIP файл проекта
        Note right of B: Берет шаблон из папки templates/vite-react и настраивает его
        B->>B: createTemplateZip использует библиотеку archiver для сжатия
        Note right of B: Заменяет __PROJECT_ID__ на реальный ID в shipvibes-dev.js
        Note right of B: Обновляет название в package.json файле проекта
        
        B->>R2: uploadProjectTemplate загружает готовый ZIP в облачное хранилище
        Note right of R2: Файл сохраняется по пути projects/projectId/template.zip
        R2-->>B: Подтверждение успешной загрузки файла
        
        B->>B: saveTemplateAsInitialCommit запускается в фоне асинхронно
        Note right of B: Извлекает файлы из ZIP и сохраняет как первый коммит в истории
        
        B-->>U: Возвращает ссылку для скачивания готового проекта
        Note right of U: Пользователь получает ZIP файл со всеми настроенными файлами
        
        U->>B: Клик по ссылке скачивания проекта
        B->>B: GET /api/projects/projectId/download обрабатывает запрос
        B->>R2: downloadProjectTemplate получает ZIP из хранилища
        R2-->>B: Отправляет содержимое template.zip файла
        B-->>U: Браузер скачивает project.zip на компьютер пользователя
    end

    %% === 2. ЛОКАЛЬНАЯ НАСТРОЙКА ===
    rect rgb(248, 255, 248)
        Note over U,W: 2️⃣ ЛОКАЛЬНАЯ НАСТРОЙКА - пользователь разворачивает проект на своем компьютере
        U->>V: Извлекает ZIP архив и открывает папку в VSCode/Cursor
        Note right of U: Распаковывает скачанный проект в рабочую папку
        
        U->>V: Выполняет команду pnpm install в терминале
        Note right of V: Устанавливаются все зависимости проекта из package.json
        
        U->>V: Запускает команду pnpm start для старта разработки
        Note right of V: Команда start запускает два процесса одновременно через concurrently
        
        V->>V: Стартует Vite dev server на порту localhost 5173
        Note right of V: Локальный сервер разработки для просмотра сайта
        
        V->>W: Запускается node shipvibes-dev.js агент синхронизации
        Note right of W: Специальный скрипт для отслеживания изменений файлов
        
        W->>W: Создается new ShipvibesAgent с PROJECT_ID из файла
        Note right of W: Агент инициализируется с уникальным ID проекта
        
        W->>W: connectWebSocket создает соединение io с WS_URL
        Note right of W: Подключается к WebSocket серверу на порту 8080
        
        W->>WS: socket.connect устанавливает WebSocket соединение
        WS->>WS: io.on connection создает новый Socket instance
        Note right of WS: Сервер регистрирует новое подключение клиента
        
        W->>WS: emit authenticate отправляет projectId и clientType agent
        Note right of W: Агент сообщает серверу к какому проекту он относится
        
        WS->>WS: socket.on authenticate обрабатывает аутентификацию в index.ts
        Note right of WS: Проверяет права доступа и добавляет в комнату проекта
        
        WS->>WS: socket.join добавляет в комнату project projectId
        Note right of WS: Все клиенты проекта получат уведомления об изменениях
        
        WS->>WS: connectedAgents.set сохраняет socketId агента
        Note right of WS: Отслеживание активных агентов для каждого проекта
        
        WS-->>W: emit authenticated подтверждает успешную аутентификацию
        Note right of W: Агент получает подтверждение и готов к работе
        
        WS->>B: io.to broadcast отправляет agent_connected всем клиентам проекта
        Note right of B: Браузер пользователя получает уведомление о подключении агента
        
        Note over WS,N: 🚀 АВТОМАТИЧЕСКИЙ ПЕРВЫЙ ДЕПЛОЙ когда агент подключается впервые
        WS->>WS: checkAndTriggerInitialDeploy проверяет нужен ли автодеплой
        Note right of WS: Функция проверяет является ли проект новым без предыдущих деплоев
        
        WS->>DB: ProjectQueries.getProjectById получает данные проекта
        Note right of DB: Проверяет есть ли netlify_url и какой deploy_status
        DB-->>WS: Объект проекта с полями netlify_url deploy_status created_at
        
        WS->>DB: FileHistoryQueries.getProjectFileHistory проверяет есть ли коммиты
        Note right of DB: Ищет существующие коммиты в истории файлов
        DB-->>WS: Массив существующих коммитов или пустой массив
        
        alt Проект новый и пустой
            Note right of WS: Если нет netlify_url нет коммитов и проект младше 5 минут
            
            WS->>WS: extractAndSaveTemplateFiles извлекает файлы из template.zip
            Note right of WS: Скачивает шаблон из R2 и извлекает все файлы проекта
            
            WS->>R2: downloadProjectTemplate получает template.zip из хранилища
            Note right of R2: Возвращает архив с исходными файлами шаблона
            R2-->>WS: Buffer с содержимым template.zip файла
            
            WS->>WS: AdmZip.getEntries извлекает файлы из ZIP архива
            Note right of WS: Распаковывает все файлы и папки из архива шаблона
            
            WS->>WS: commitId equals crypto.randomUUID создает ID первого коммита
            Note right of WS: Генерирует уникальный идентификатор для initial commit
            
            WS->>R2: uploadFileVersion сохраняет каждый файл шаблона в versions
            Note right of R2: Загружает все файлы по пути projects/projectId/versions/commitId/
            R2-->>WS: Подтверждения загрузки всех файлов шаблона
            
            WS->>DB: FileHistoryQueries.createFileHistory создает записи для всех файлов
            Note right of DB: Каждый файл шаблона получает запись в file_history с commit_id
            DB->>DB: INSERT INTO file_history множественные записи с commit_message Initial commit
            
            WS->>WS: triggerDeploy запускает первый деплой проекта
            Note right of WS: Автоматически деплоит extracted файлы шаблона на Netlify
            
            Note over WS,N: Дальше идет обычный процесс Netlify деплоя как в секции 5
            WS->>WS: ensureNetlifySite создает сайт на Netlify
            WS->>N: NetlifyService.createSite POST запрос к Netlify API
            N-->>WS: Возвращает siteId и URL нового сайта
            WS->>WS: Процесс сборки и деплоя как в секции 5 NETLIFY DEPLOY PROCESS
            
        else Проект уже имеет деплои
            Note right of WS: Если есть netlify_url или существуют коммиты - пропускаем автодеплой
            WS->>WS: Пропускает автоматический деплой и продолжает обычную работу
        end
        
        W->>W: startFileWatcher запускает chokidar.watch с настройками
        Note right of W: Начинает следить за всеми файлами кроме node_modules .git dist
    end

    %% === 3. РЕДАКТИРОВАНИЕ ФАЙЛОВ ===
    rect rgb(255, 248, 240)
        Note over U,DB: 3️⃣ РЕДАКТИРОВАНИЕ ФАЙЛОВ - TRACKING STAGE отслеживание изменений без коммитов
        
        U->>V: Открывает и редактирует файл src/App.tsx в редакторе
        Note right of U: Пользователь вносит изменения в код проекта
        
        V->>W: chokidar.on change срабатывает при сохранении файла
        Note right of W: Библиотека chokidar детектирует изменение в файловой системе
        
        W->>W: handleFileChange обрабатывает событие изменения
        Note right of W: Читает новое содержимое файла и готовит к отправке
        
        W->>W: fs.readFileSync читает новое содержимое файла
        Note right of W: Получает актуальный текст из измененного файла
        
        W->>W: crypto.createHash sha256 вычисляет хеш содержимого
        Note right of W: Создает уникальный отпечаток файла для проверки изменений
        
        W->>WS: emit file_change отправляет данные об изменении
        Note right of W: Передает projectId filePath content hash на сервер
        
        WS->>WS: socket.on file_change получает событие в index.ts
        Note right of WS: Сервер обрабатывает входящее изменение файла
        
        WS->>DB: PendingChangesQueries.upsertPendingChange сохраняет изменение
        Note right of DB: Временно сохраняет изменение без создания постоянного коммита
        
        DB->>DB: INSERT или UPDATE pending_changes ON CONFLICT project_id file_path
        Note right of DB: Если файл уже изменялся то обновляет иначе создает новую запись
        Note right of DB: ВАЖНО НЕ создается постоянный коммит в file_history
        
        DB-->>WS: Возвращает сохраненную запись с id file_path action modified
        
        WS-->>W: emit file_tracked подтверждает получение и сохранение
        Note right of W: Агент получает подтверждение что изменение зафиксировано
        
        WS->>B: socket.to broadcast отправляет file_updated другим клиентам
        Note right of B: Другие открытые вкладки браузера получают уведомление об изменении
        
        Note over U,DB: ⏩ ПРОЦЕСС ПОВТОРЯЕТСЯ для каждого измененного файла
        U->>V: Редактирует другой файл src/components/Header.tsx
        V->>W: chokidar.on change снова срабатывает
        W->>WS: emit file_change отправляет новое изменение
        WS->>DB: upsertPendingChange накапливает еще одно изменение
        Note right of DB: Все изменения накапливаются в таблице pending_changes
    end

    %% === 4. ДЕПЛОЙ ПРОЦЕСС ===
    rect rgb(255, 240, 245)
        Note over U,N: 4️⃣ ДЕПЛОЙ ПРОЦЕСС - COMMIT STAGE создание постоянного снапшота и деплой
        
        U->>B: Открывает проект в браузере и видит Pending Changes Sidebar
        Note right of U: В боковой панели отображаются все несохраненные изменения
        
        U->>B: Нажимает кнопку Save All в PendingChangesSidebar компоненте
        Note right of U: Пользователь решает сохранить накопленные изменения и задеплоить
        
        B->>B: handleSaveAll вызывает saveAllChanges из контекста проекта
        Note right of B: React компонент вызывает функцию сохранения всех изменений
        
        B->>WS: WebSocket emit save_all_changes отправляет команду на сервер
        Note right of B: Браузер через WebSocket просит сервер создать коммит
        
        WS->>WS: socket.on save_all_changes обрабатывает запрос в index.ts
        Note right of WS: Сервер получает команду на создание полного снапшота
        
        WS->>WS: saveFullProjectSnapshot создает полный снапшот проекта
        Note right of WS: Ключевая функция объединяющая все изменения в один коммит
        
        WS->>DB: PendingChangesQueries.getAllPendingChanges читает все изменения
        Note right of DB: Получает все временные изменения для данного проекта
        DB-->>WS: Массив объектов с file_path content action для каждого файла
        
        WS->>DB: FileHistoryQueries.getProjectFileHistory читает последние версии
        Note right of DB: Получает последние сохраненные версии всех файлов проекта
        DB-->>WS: Последние версии файлов из предыдущих коммитов
        
        WS->>WS: Merge pending + existing files создает fullSnapshot Map
        Note right of WS: Объединяет временные изменения с существующими файлами
        Note right of WS: Получается полный снапшот всех файлов текущего состояния проекта
        
        WS->>WS: commitId equals crypto.randomUUID генерирует уникальный ID коммита
        Note right of WS: Создает уникальный идентификатор для этого снапшота
        
        WS->>R2: uploadFileVersion для каждого файла в облачное хранилище
        Note right of R2: Сохраняет ВСЕ файлы проекта по пути projects/projectId/versions/commitId/filePath
        R2-->>WS: Подтверждения загрузки для всех файлов
        
        WS->>DB: FileHistoryQueries.createFileHistory для каждого файла
        Note right of DB: Создает записи в таблице file_history с commit_id и r2_object_key
        DB->>DB: INSERT INTO file_history множественные записи с одинаковым commit_id
        Note right of DB: Все файлы снапшота связаны одним commit_id
        
        WS->>DB: PendingChangesQueries.clearAllPendingChanges очищает временные изменения
        Note right of DB: Удаляет все записи из pending_changes так как они уже сохранены
        DB->>DB: DELETE FROM pending_changes WHERE project_id equals projectId
        
        WS->>WS: triggerDeploy запускает процесс деплоя на Netlify
        Note right of WS: Переходит к следующему этапу развертывания на боевом сервере
    end

    %% === 5. NETLIFY ДЕПЛОЙ ===
    rect rgb(240, 255, 240)
        Note over WS,N: 5️⃣ NETLIFY DEPLOY PROCESS процесс сборки и деплоя на продакшн
        
        WS->>WS: ensureNetlifySite проверяет есть ли сайт на Netlify
        Note right of WS: Проверяет есть ли уже созданный сайт для этого проекта
        
        WS->>N: NetlifyService.createSite создает новый сайт если нужно
        Note right of N: POST запрос к Netlify API с именем shipvibes-projectId
        N-->>WS: Возвращает siteId и netlifyUrl нового сайта
        
        WS->>DB: ProjectQueries.updateProject сохраняет данные Netlify
        Note right of DB: Обновляет поля netlify_site_id и netlify_url в таблице projects
        
        WS->>WS: NetlifyService.deployProject запускает процесс деплоя
        Note right of WS: Начинает полный цикл сборки и развертывания проекта
        
        WS->>WS: createProjectZipBuffer создает ZIP архив для деплоя
        Note right of WS: Ключевая функция восстановления и сборки проекта
        
        WS->>WS: getLatestProjectFiles получает файлы текущего коммита
        Note right of WS: Определяет какие файлы входят в деплоимый снапшот
        
        WS->>DB: FileHistoryQueries.getProjectFileHistory получает список файлов коммита
        Note right of DB: Возвращает все записи file_history для данного commit_id
        
        WS->>R2: downloadTextFile скачивает содержимое каждого файла по r2_object_key
        Note right of R2: Восстанавливает актуальное содержимое всех файлов проекта
        
        WS->>WS: fs.mkdtemp создает временную директорию для сборки
        Note right of WS: Создает изолированную папку для восстановления и сборки проекта
        
        WS->>WS: Restore all files восстанавливает все файлы в временной директории
        Note right of WS: Записывает все файлы проекта в правильной структуре папок
        
        WS->>WS: restoreTemplateFiles если package.json отсутствует
        Note right of WS: Восстанавливает недостающие файлы из исходного шаблона
        
        WS->>WS: execAsync pnpm install устанавливает зависимости проекта
        Note right of WS: Выполняет команду установки всех npm пакетов в временной папке
        
        WS->>WS: execAsync pnpm build собирает проект для продакшн
        Note right of WS: Запускает Vite build который создает оптимизированные файлы в папке dist
        
        WS->>WS: AdmZip.addDirectoryToZip создает ZIP только из папки dist
        Note right of WS: Упаковывает только собранные статические файлы готовые для хостинга
        
        WS->>N: POST sites/siteId/deploys загружает ZIP через Netlify API
        Note right of N: Отправляет архив с Content-Type application/zip на сервера Netlify
        N-->>WS: Возвращает deployId deploy_url и state building
        
        WS->>DB: ProjectQueries.updateProject обновляет статус деплоя
        Note right of DB: Устанавливает deploy_status building и netlify_deploy_id
        
        WS->>WS: setTimeout запускает проверку статуса через 5 секунд
        Note right of WS: Планирует периодическую проверку готовности деплоя
    end

    %% === 6. МОНИТОРИНГ ===
    rect rgb(240, 255, 240)
        Note over WS,N: 6️⃣ МОНИТОРИНГ ДЕПЛОЯ отслеживание статуса и уведомление пользователя
        
        loop Каждые 30 секунд через setInterval
            WS->>WS: NetlifyService.checkAllActiveDeployments проверяет все активные деплои
            Note right of WS: Проходит по всем проектам в состоянии building
            
            WS->>N: GET sites/siteId/deploys/deployId запрашивает текущий статус
            Note right of N: Netlify возвращает актуальное состояние деплоя
            N-->>WS: Объект с полями state ready building error и deploy_url
            
            alt state равен ready
                WS->>DB: ProjectQueries.updateProject устанавливает deploy_status ready
                Note right of DB: Помечает проект как успешно задеплоенный
                
                WS->>B: io.to projectId emit deploy_status_update уведомляет браузер
                Note right of B: Все открытые вкладки проекта получают уведомление об успехе
                
            else state равен error
                WS->>DB: ProjectQueries.updateProject устанавливает deploy_status failed
                Note right of DB: Помечает деплой как неудачный
                
                WS->>B: io.to projectId emit deploy_status_update уведомляет об ошибке
                Note right of B: Браузер показывает пользователю сообщение о проблеме
            end
        end
        
        Note over U,N: 🎉 ФИНАЛЬНЫЙ РЕЗУЛЬТАТ пользователь получает рабочий сайт
        N-->>U: Сайт становится доступен по netlify URL в интернете
        Note right of U: Любой может открыть ссылку и увидеть обновленный сайт
        
        B->>U: Браузер показывает уведомление Deploy successful
        Note right of U: Пользователь видит в интерфейсе что деплой завершен успешно
        
        U->>U: Открывает живой сайт в новой вкладке браузера
        Note right of U: Пользователь может убедиться что все изменения применились
    end