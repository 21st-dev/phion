---
description:
globs:
alwaysApply: false
---
# Geist UI Components Documentation

Полная библиотека компонентов в стиле Vercel Geist UI для платформы Shipvibes.dev.

## 📱 Basic Components

### Pagination
**Файл:** `pagination.tsx`
**Назначение:** Навигация между страницами с кнопками Previous/Next
**Использование:** 
```tsx
<Pagination 
  previous={{ title: "Intro", href: "/intro" }}
  next={{ title: "Setup", href: "/setup" }}
/>
```

### Description
**Файл:** `description.tsx`
**Назначение:** Отображение пары title/content с опциональным tooltip
**Использование:**
```tsx
<Description 
  title="Project Name" 
  content="My React App"
  tooltip="Unique project identifier"
/>
```

### StatusDot
**Файл:** `status-dot.tsx`
**Назначение:** Индикатор статуса деплоя (QUEUED, BUILDING, ERROR, READY, CANCELED)
**Использование:**
```tsx
<StatusDot state="READY" label={true} />
```

## 🎨 Layout and Material Components

### Material
**Файл:** `material.tsx`
**Назначение:** Контейнер с различными типами теней и скругления
**Типы:** base, small, medium, large, tooltip, menu, modal, fullscreen
**Использование:**
```tsx
<Material type="modal" className="p-4">
  <p>Modal content</p>
</Material>
```

## 📝 Form Components

### Checkbox
**Файл:** `checkbox.tsx`
**Назначение:** Чекбокс с поддержкой indeterminate состояния
**Особенности:** Hover эффекты, disabled состояние
**Использование:**
```tsx
<Checkbox 
  checked={isChecked} 
  onChange={setIsChecked}
  indeterminate={someSelected}
>
  Select all
</Checkbox>
```

### Slider
**Файл:** `slider.tsx`
**Назначение:** Range slider с кастомным стилем
**Особенности:** Поддержка темной темы, кастомный thumb
**Использование:**
```tsx
<Slider value={value} onValueChange={setValue} />
```

### Toggle
**Файл:** `toggle.tsx`
**Назначение:** Toggle switch с различными цветами и размерами
**Размеры:** small, large
**Цвета:** blue, red, amber, green, teal, purple, pink, gray
**Использование:**
```tsx
<Toggle 
  checked={isEnabled} 
  onChange={setIsEnabled}
  color="blue"
  size="large"
>
  Enable notifications
</Toggle>
```

### Select
**Файл:** `select.tsx`
**Назначение:** Dropdown select с различными размерами и состояниями
**Размеры:** xsmall, small, medium, large
**Особенности:** Prefix/suffix иконки, error состояние
**Использование:**
```tsx
<Select 
  options={[{value: "react", label: "React"}]}
  value={selected}
  onChange={handleChange}
  size="medium"
  error="Required field"
/>
```

### ChoiceboxGroup
**Файл:** `choicebox-group.tsx`
**Назначение:** Группа radio buttons или checkboxes с карточным дизайном
**Типы:** radio, checkbox
**Направления:** row, column
**Использование:**
```tsx
<ChoiceboxGroup type="radio" direction="column" value={value} onChange={setValue}>
  <ChoiceboxGroup.Item title="Option 1" description="Description" value="opt1" />
</ChoiceboxGroup>
```

## 📄 Content Components

### ShowMore
**Файл:** `show-more.tsx`
**Назначение:** Кнопка для раскрытия/сворачивания контента
**Особенности:** Анимированная стрелка, customizable className
**Использование:**
```tsx
<ShowMore expanded={isExpanded} onClick={setIsExpanded} />
```

### Tooltip
**Файл:** `tooltip.tsx`
**Назначение:** Всплывающие подсказки с разными стилями
**Типы:** success, warning, error, violet, default
**Позиции:** top, bottom, left, right
**Использование:**
```tsx
<Tooltip text="This is a tooltip" type="success" position="top">
  <button>Hover me</button>
</Tooltip>
```

### ContextCard
**Файл:** `context-card.tsx`
**Назначение:** Контекстные карточки с rich content в tooltip
**Использование:**
```tsx
<ContextCard.Trigger content={<div>Rich content</div>} side="top">
  <span>Trigger element</span>
</ContextCard.Trigger>
```

### RelativeTimeCard
**Файл:** `relative-time-card.tsx`
**Назначение:** Отображение относительного времени с detailed tooltip
**Особенности:** Автообновление каждую секунду, UTC и local time
**Использование:**
```tsx
<RelativeTimeCard date={timestamp} side="top">
  <span>2 hours ago</span>
</RelativeTimeCard>
```

## 🧭 Navigation and Interface

### Tabs
**Файл:** `tabs.tsx`
**Назначение:** Табы с двумя вариантами дизайна
**Варианты:** primary (с border), secondary (с background)
**Особенности:** Поддержка иконок, tooltip, disabled состояние
**Использование:**
```tsx
<Tabs 
  selected={activeTab} 
  setSelected={setActiveTab}
  variant="primary"
  tabs={[
    { title: "Overview", value: "overview" },
    { title: "Settings", value: "settings", disabled: true }
  ]}
/>
```

### ThemeSwitcher
**Файл:** `theme-switcher.tsx`
**Назначение:** Переключатель темы (system, light, dark)
**Особенности:** Автоматическое определение system preference, localStorage
**Использование:**
```tsx
<ThemeSwitcher />
```

## 📊 Data Display

### Avatar
**Файл:** `avatar.tsx`
**Назначение:** Аватары пользователей с различными вариантами
**Компоненты:**
- `Avatar` - базовый аватар
- `GitHubAvatar` - с GitHub иконкой
- `GitLabAvatar` - с GitLab иконкой  
- `BitbucketAvatar` - с Bitbucket иконкой
- `AvatarGroup` - группа аватаров с лимитом
**Использование:**
```tsx
<Avatar src="avatar.jpg" size={32} />
<GitHubAvatar username="octocat" src="avatar.jpg" />
<AvatarGroup members={users} limit={3} size={24} />
```

### Gauge
**Файл:** `gauge.tsx`
**Назначение:** Круговой прогресс индикатор
**Размеры:** tiny, small, medium, large
**Особенности:** Кастомные цвета, indeterminate состояние, arc priority
**Использование:**
```tsx
<Gauge 
  value={75} 
  size="medium"
  showValue={true}
  colors={{ "0": "#red", "50": "#yellow", "80": "#green" }}
/>
```

### Skeleton
**Файл:** `skeleton.tsx`
**Назначение:** Скелетон загрузки с анимацией
**Формы:** pill, rounded, squared
**Особенности:** Может оборачивать контент, анимация отключаемая
**Использование:**
```tsx
<Skeleton width={200} height={24} animated={true} pill={true} />
<Skeleton show={loading}>
  <div>Content that loads</div>
</Skeleton>
```

## 💻 Code and Development

### CodeBlock
**Файл:** `code-block.tsx`
**Назначение:** Блок кода с подсветкой синтаксиса
**Особенности:** Filename, language switcher, copy button, line numbers, highlighted lines
**Поддерживаемые языки:** js, jsx, ts, tsx, next, и другие
**Использование:**
```tsx
<CodeBlock 
  filename="App.tsx"
  language="tsx"
  highlightedLinesNumbers={[1, 5, 10]}
  switcher={{
    options: [{value: "tsx", label: "TypeScript"}],
    value: "tsx",
    onChange: setLanguage
  }}
>
  {codeString}
</CodeBlock>
```

### Snippet
**Файл:** `snippet.tsx`
**Назначение:** Копируемые фрагменты кода/команд
**Типы:** success, warning, error
**Особенности:** Prompt ($), copy button с анимацией
**Использование:**
```tsx
<Snippet 
  text={["npm install", "npm start"]}
  type="success"
  prompt={true}
  onCopy={() => console.log('Copied')}
/>
```

## 🔄 Feedback and Interaction

### Button
**Файл:** `button.tsx`
**Назначение:** Кнопки с различными стилями и состояниями
**Типы:** primary, secondary, tertiary, error, warning
**Размеры:** tiny, small, medium, large
**Формы:** square, circle, rounded
**Особенности:** Loading состояние, prefix/suffix иконки, fullWidth, shadow
**Использование:**
```tsx
<Button 
  type="primary" 
  size="medium"
  loading={isLoading}
  prefix={<Icon />}
  shadow={true}
  onClick={handleClick}
>
  Save Changes
</Button>
```

### Spinner
**Файл:** `spinner.tsx`
**Назначение:** Индикатор загрузки с анимированными барами
**Особенности:** Кастомный размер и цвет
**Использование:**
```tsx
<Spinner size={24} color="#0070f3" />
```

## 🎨 Design System Variables

### CSS Custom Properties

```css
/* Основные цвета */
--ds-gray-100 до --ds-gray-1000
--ds-background-100, --ds-background-200

/* Цветные варианты для toggles */
--ds-blue-100, --ds-blue-700, --ds-blue-1000
--ds-red-100, --ds-red-600, --ds-red-1000
--ds-amber-100, --ds-amber-700, --ds-amber-1000
--ds-green-100, --ds-green-700, --ds-green-1000
--ds-teal-100, --ds-teal-700, --ds-teal-1000
--ds-purple-100, --ds-purple-700, --ds-purple-1000
--ds-pink-100, --ds-pink-700, --ds-pink-1000

/* Тени для Material компонента */
--ds-shadow-border, --ds-shadow-small
--ds-shadow-border-small, --ds-shadow-medium
--ds-shadow-border-medium, --ds-shadow-large
--ds-shadow-border-large, --ds-shadow-tooltip
--ds-shadow-menu, --ds-shadow-modal, --ds-shadow-fullscreen

/* Geist брендинг */
--geist-error, --geist-warning, --geist-cyan
--geist-success, --geist-violet, --geist-foreground
--geist-background, --geist-secondary

/* Accent цвета */
--accents-1, --accents-2

/* Context cards */
--context-card-border
```

## 🔧 TypeScript Types

### Основные типы:

```typescript
// Tabs
interface ITab {
  title?: string;
  value: string;
  disabled?: boolean;
  icon?: string;
  tooltip?: string;
}

// Select
interface Option {
  value: string;
  label: string;
}
```

## 📱 Responsive Design

Все компоненты поддерживают:
- ✅ Dark mode через CSS переменные
- ✅ Responsive поведение
- ✅ Touch-friendly интерфейсы
- ✅ Accessibility (ARIA attributes)
- ✅ Keyboard navigation

## 🎯 Best Practices

1. **Используйте TypeScript типы** из экспорта для type safety
2. **Следуйте naming convention** компонентов Geist
3. **CSS переменные** автоматически поддерживают темную тему
4. **Анимации** включены по умолчанию, но можно отключить
5. **Accessibility** встроен во все компоненты

## 🚀 Usage Example

```tsx
import { 
  Button, 
  CodeBlock, 
  Tabs, 
  StatusDot, 
  ThemeSwitcher,
  type ITab 
} from "@/components/geist";

const tabs: ITab[] = [
  { title: "Overview", value: "overview" },
  { title: "Code", value: "code" }
];

export function ProjectPage() {
  return (
    <div className="space-y-4">
      <div className="flex justify-between items-center">
        <StatusDot state="READY" label />
        <ThemeSwitcher />
      </div>
      
      <Tabs selected={activeTab} setSelected={setActiveTab} tabs={tabs} />
      
      <CodeBlock filename="App.tsx" language="tsx">
        {code}
      </CodeBlock>
      
      <Button type="primary" size="medium">
        Deploy Project
      </Button>
    </div>
  );
}
```

Эта библиотека обеспечивает полный набор компонентов для создания современного интерфейса в стиле Vercel.
